///|
/// Integration tests — requires API keys in environment variables.
/// These test chat_gateway() with real providers.
/// Run with: moon test --target native -p trkbt10/llm_interop/client

// Integration tests for chat_gateway() will be added when HttpTransport
// mock infrastructure is available. Current integration testing is done
// at the mukuro level via RuntimeModelOrchestrator tests.
