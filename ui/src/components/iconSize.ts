/** Icon sizes by usage context */
export const ICON_SIZE = {
  /** Inside BlockLabel, inline with small text */
  label: 12,
  /** Message action buttons */
  action: 14,
  /** Toolbar buttons (input area) */
  toolbar: 16,
} as const;
